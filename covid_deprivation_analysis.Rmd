---
title: "R Notebook"
output: html_notebook
---

```{r}
plot(df_ane_hscp_dep$value~data$date , type="b" , lwd=3 , col=rgb(0.1,0.7,0.1,0.8) , ylab="value of ..." , xlab="date" , bty="l" , pch=20 , cex=4)
abline(h=seq(0,100,10) , col="grey", lwd=0.8)
```

```{r}
df_cov_hb_dep
```

```{r}
day_case <- day_cum_case %>% 
  mutate(year = year(date),
         month = month(date),
           week = week(date))

cov_case_rate <- day_case %>% 
  group_by(year,month) %>% 
  mutate(rate = daily_cases - lag(daily_cases)) %>% 
  slice(-1)


cov_case_rate %>% 
  mutate(month = factor(month)) %>% 
  ggplot()+
  aes(x = date, y = daily_cases )+
  geom_bar(stat = "identity")+
  labs(
    title = "Daily Covid Cases Per month"
  )
```

```{r}
day_cum_case <- read_csv("clean_data/df_day_cum_cov.csv")
```

